import type {
  ScaleConfig,
  StyleConfig,
} from './types';

export const DEFAULT_STYLE_CONFIG: StyleConfig = {
  radiusTopLeft: 1,
  radiusTopRight: 1,
};

export const DEFAULT_SCALE_CONFIG: ScaleConfig = {
  y: {
    zero: true,
  },
};
